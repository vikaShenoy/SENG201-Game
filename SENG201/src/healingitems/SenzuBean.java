package healingitems;

/**
 * @author vsh33 SenzuBean is a subclass of HealingItems. SenzuBean is one
 *         of the healing items in the game, which the hero can consume to
 *         regain health.
 */
public class SenzuBean extends HealingItems {
	/**
	 * Creates a SenzuBean.
	 */
	public SenzuBean() {
		super("Senzu Bean", 200, 100, 10);
	}
}
